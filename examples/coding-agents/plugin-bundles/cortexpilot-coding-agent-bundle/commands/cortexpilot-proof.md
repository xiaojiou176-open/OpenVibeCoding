# CortexPilot proof-first review

Use CortexPilot as the proof-and-replay layer around the current task.

1. Read `README.md`, `docs/README.md`, and `AGENTS.md` first.
2. Route to the public compatibility / integration / MCP guides before deeper
   implementation.
3. Keep hosted, write-capable MCP, and official plugin claims frozen unless the
   repo truth explicitly changes.
4. Prefer the read-only MCP path when the first need is inspection.
5. Prefer Workflow Cases, Proof & Replay, and builder entrypoints when the task
   needs reviewable operator evidence.
