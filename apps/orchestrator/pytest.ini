[pytest]
pythonpath = src
addopts = --strict-markers --import-mode=importlib
cache_dir = ../../.runtime-cache/cache/test/orchestrator-pytest
markers =
    e2e: end-to-end tests
    serial: tests that must run without xdist parallel workers
    slow: long-running tests that should run after fast gates
